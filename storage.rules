rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow admin to read, write, and delete all files.
    match /{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.token.email == 'quizpankaj@gmail.com';
    }
  }
}
